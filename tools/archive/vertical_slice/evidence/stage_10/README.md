Stage 10 runtime evidence output directory.

Expected outputs when `tools/stage_10_runtime_harness.tscn` is executed:
- numbered screenshots for town, mine, battle, cutscene, follower, and release-gate smoke captures
- `runtime_results.json` with cue-routing, SFX smoke, flag, and flow results

If Godot CLI is unavailable, collect the same captures manually in-editor and save them here with matching names where practical.
